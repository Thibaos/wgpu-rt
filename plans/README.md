# Implementation Plans

Backlog for the wgpu-rt engine, generated by the improve skill (2026-07-06).
This file is the **status board** — one word per plan, and the open findings.
The paper trail (execution notes, reconcile passes, measurements, rejected
ideas) lives in [HISTORY.md](HISTORY.md); per-plan detail lives in the plan
files.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (why).

## Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Recreate RT output texture on window resize | P1 | M | — | DONE |
| 002  | Handle surface acquire timeout and outdated gracefully | P1 | S | — | DONE |
| 003  | Chunked world architecture — binary format, bake tool, multi-chunk rendering | P1 | L | — | DONE |
| 004  | Add color palette from .vox file to .world format and GPU rendering | P1 | M | — | DONE |
| 005  | Replace tree64 with dense 3D-texture DDA ray marcher | P1 | L | — | REJECTED — unsuitable for 16384³ scenes |
| 006  | Build Tree64 GPU data from occupied voxels | P1 | L | — | DONE |
| 007  | FPS player controller with world collision | P1 | L | — | REJECTED — rationale unrecorded (see plan) |
| 008  | Stop requesting all experimental GPU features | P1 | S | — | DONE |
| 009  | Handle cursor-grab errors instead of panicking | P2 | S | — | DONE |
| 010  | Wire the loaded chunked world into the voxel renderer | P1 | L | 008, 009 | DONE |
| 011  | Hierarchical mip DDA traversal — phase 2 | P1 | L | — | DONE |
| 012  | Debug orbit camera for reliable scene coverage | P1 | S | — | DONE |
| 013  | Complete plan 011 — hierarchical mip DDA CPU reference tests | P1 | M | 011 | REJECTED — deliverable existed; committed in `cc56462` |
| 014  | Primary-view latency — instrumentation, orbit, early-out + tmax, tight AABBs, chunk-size matrix | P1 | L | 012, Design A ray-query | REJECTED — superseded by 015 |
| 015  | 8³-chunk rewrite — storage-buffer voxel pool, flat-DDA ray-query primary, retire raster (supersedes 014) | P0 | XL | 012, 019 | TODO — **dispatchable** (drift check verified 2026-08-03 @ `c151709`) |
| 016  | Measure the DDA render pass with GPU timestamps, then apply the two highest-leverage shader optimizations | P1 | M | — | DONE — measured; Optim. A no gain, reverted |
| 017  | Replace per-proxy fragment DDA with a single cross-chunk fullscreen pass | P1 | L | 016 | BLOCKED — probe gate: <9% fragment work at MED-HIGH risk |
| 018  | Depth-exit — terminate the chunk DDA at the previous frame's nearest surface | P1 | M | 016 | REJECTED — obsoleted by 019 (fragment path retired) |
| 019  | Design A — TLAS-of-chunk-AABBs ray-query renderer (hardware RT primary pass) | P0 | L | wgpu 30 ray-query | DONE — 2.4–3.8x faster primary (`f014d3b`) |
| 020  | Stopgap: fix silent world clipping — grid-centered loading, CHUNKS_Y=8, drop diagnostics | P1 | M | — | DONE — merged `d3304c2`; superseded structurally by 015 |
| 021  | Refresh plan 015 — drift check, Current state, Stage-5 notes (post-020/022) | P1 | S | — | DONE |
| 022  | Restore the traversal heatmap — per-pixel DDA-work coloring in both renderers | P2 | S | — | DONE — merged `09e5981` |

## Next actions

1. **Dispatch plan 015** (P0). Drift check verified live 2026-08-03 against
   HEAD `c151709` — every Current-state excerpt matches, all dependencies
   DONE and merged. The executor re-verifies at dispatch anyway (the plan's
   own drift check).
2. **Plan a small fix for finding #10** (clippy `--all-targets` red on the
   two integration test files — missing `#[allow]` blocks; lib/bin clean).
3. Pick up P2 findings below when 015 lands: CI, README, controller tests.

## Open findings

| # | Finding | Category | Priority |
|---|---------|----------|----------|
| 3  | Thin test coverage: player controller (only orbit math tested), framework, render path untested | Tests | P2 |
| 5  | No CI, no fmt/clippy in automation | DX | P2 |
| 6  | No README | Docs | P3 |
| 10 | `cargo clippy --all-targets -- -D warnings` is RED on the integration tests (71 in `hierarchical_mip_dda.rs`, 8 in `shader_validate.rs` — missing `#[allow]` blocks; lib/bin clean). Plans gate on this command; claims of "clippy clean" are false unless scoped to lib/bin | Tests/DX | P1 |
| 11 | player_controller `fly_movement`/`rotate`/`handle_speed_change` untested | Tests | P2 |
| 12 | No cargo-audit coverage; not installed in this environment | Dependencies | P3 |

## Dependencies

- 015 depends on 012 and 019 (both DONE); 020/022 must be merged before
  015's Stage-5 measurement sweep so it runs on unclipped scenes (both
  merged at `d3304c2` / `09e5981`).
- Everything else is terminal — dependency edges for executed plans are
  historical (see HISTORY.md).

## History

See [HISTORY.md](HISTORY.md): reconcile passes, execution notes with
measurement outcomes (016/019/020/022), the 011 correction, resolved and
rejected findings. **Executors: do not edit this README or HISTORY.md — your
reviewer records outcomes.**
