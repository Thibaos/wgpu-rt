# Advisor Implementation Plans

Generated by the improve skill on 2026-07-31. These plans live outside the
repo's own `plans/` workflow (which is maintained by the project's reviewer
process); they are advisory, and each executor updates their own row here.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Measure the DDA render pass with GPU timestamps, then apply the two highest-leverage shader optimizations | P1 | M | — | BLOCKED (2026-07-31: executed; instrumentation verified & kept; Optim. A measured no gain — 40.06→39.40/40.41ms GPU — gate failed, reverted; B not run per gate. Durable asset = GPU timestamps + heatmap diff, uncommitted in worktree /tmp/wgpu-rt-exec) — 2026-08-01: instrumentation rebuilt in-tree (env-gated timestamps + DDA work counters) and committed with a headless bench; release-mode measurements: monu1 20-23ms GPU (~45fps), bistro_sm 70-89ms (~12fps), church 58-105ms; render pass is GPU-bound, latency-bound (400-860M cells/s), not bandwidth-bound |
| 002  | Replace per-proxy fragment DDA with a single cross-chunk fullscreen pass (probe redundancy first, then A/B-gated nearest-wins rewrite) | P1 | L | plan 001 instrumentation (Step 0) | BLOCKED (2026-07-31: Step-1 probe gate STOPPED the build. Measured invocation_ratio avg 1.09 ≤ 1.15 — only ~9% per-pixel overlap in the orbit sweep (covered_ratio 0.20-0.28/proxy, samples/fragment 12.8). Fullscreen rewrite would save <9% of fragment work at MED-HIGH risk; plan's gate says STOP and pivot. Probe fully reverted, tree at plan-001 state, all 27 gates pass. Next lever: half-res DDA + upscale or compute path — both stack on plan-001 instrumentation.) — 2026-08-01: independently re-measured in release: overdraw 1.00-1.24x px across monu1/bistro_sm/church (confirms probe); early-Z experiment (frag_depth removal + front-to-back sort) measured no GPU-time gain — depth restored; direction set: half-res DDA + depth-term or compute path |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None yet (single plan).

## Findings considered and rejected

- *Cross-chunk traversal / occlusion to remove per-proxy pixel redundancy*:
  the biggest architectural lever (frag_depth disables early-Z, so overlap
  pixels run the full DDA once per covering proxy), but ADR-0002 explicitly
  defers cross-chunk traversal. Not planned; noted as a measured-direction
  candidate once plan 001's instrumentation exists.
- *Half-resolution DDA + upscale*: ~4× fewer fragments but a visible quality
  trade-off; not requested, deferred.
- *Compute-shader rewrite*: full architectural rewrite, high risk; deferred.
- *Heatmap feature as a standalone plan*: folded into plan 001 as a
  diagnostics step (it is dead code today — `viewport_and_heatmap` is declared
  but never read — and wiring it costs ~15 lines).
